{
    "require": {
        "geoip2/geoip2": "^3.3"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.0",
        "php-stubs/wordpress-stubs": "^7.0"
    },
    "scripts": {
        "phpstan": "phpstan analyse -c phpstan.neon --memory-limit=4G"
    }
}